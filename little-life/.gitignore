node_modules
dist
.DS_Store
*.local

# 원본 에셋 시트는 무겁다 (15장 40MB). 개별 아이템 파일만 저장소에 둔다.
# assets/source-sheets/README.md 참고.
assets/source-sheets/*.png
# 다만 extra.png 는 2MB 뿐이고, 이 여섯 개 그림의 유일한 사본이라 함께 둔다.
!assets/source-sheets/extra.png
assets/slices/

# 그린 SVG 를 webp 로 만드는 중간 단계
assets/drawn/.rendered/
